server:
  port: 8080

spring:
  application:
    name: goods-service

  datasource:
    url: jdbc:mysql://localhost:3306/trade_logistics?useSSL=false&serverTimezone=UTC
    username: root
    password: Vinu@2003
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

